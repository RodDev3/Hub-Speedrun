{% extends '../base.html.twig' %}

{% block title %}Supports{% endblock %}

{% block body %}
    <h1>Supports</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ support.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ support.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_supports_supports_index') }}">back to list</a>

    <a href="{{ path('app_supports_supports_edit', {'id': support.id}) }}">edit</a>

    {{ include('supports/supports/_delete_form.html.twig') }}
{% endblock %}
