{% extends '../base.html.twig' %}

{% block title %}Edit Runs{% endblock %}

{% block body %}
    <h1>Edit Runs</h1>

    {{ include('runs/runs/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_runs_runs_index') }}">back to list</a>

    {{ include('runs/runs/_delete_form.html.twig') }}
{% endblock %}
