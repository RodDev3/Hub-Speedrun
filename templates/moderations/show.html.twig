{% extends '../base.html.twig' %}

{% block title %}Moderations{% endblock %}

{% block body %}
    <h1>Moderations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moderation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_moderations_moderations_index') }}">back to list</a>

    <a href="{{ path('app_moderations_moderations_edit', {'id': moderation.id}) }}">edit</a>

    {{ include('moderations/moderations/_delete_form.html.twig') }}
{% endblock %}
