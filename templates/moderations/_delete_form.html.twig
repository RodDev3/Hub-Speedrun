<form method="post" action="{{ path('app_moderations_moderations_delete', {'id': moderation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moderation.id) }}">
    <button class="btn">Delete</button>
</form>
